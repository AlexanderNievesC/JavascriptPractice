function twoSum(numbers, target) {
    let new_numbers= String(numbers).split("")
    let suma= [];

    new_numbers.map(element=>{
        for(i=0;i<new_numbers.length;i++){
            console.log(element)
            }
        }
    )
    
    return suma
}

console.log(twoSum([1, 2, 3], 4) )